{
  "name": "avyra-fullstack",
  "version": "1.0.0",
  "description": "Full-stack Avyra agency website built with React, Tailwind CSS, MongoDB, and Web3Forms.",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
